# This is a CMAKE file for the src/ directory. The src directory should hold 
# all of the .c and .cpp files as oppsoed to the include/ directory which holds 
# the headers.

set(SRC_FILES 
    pdfMerger.cpp
)
